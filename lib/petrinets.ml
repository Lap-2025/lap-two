open Utils

type place = unit
type transition = unit
type arc = unit
type petri_net = unit

let empty = assert false

let mk_place = assert false

let mk_transition = assert false

let mk_outgoing_arc = assert false

let mk_incoming_arc = assert false

let add_place = assert false

let add_transition = assert false

let add_arc = assert false

let marking_of_place = assert false

let preceding_places = assert false

let succeeding_places = assert false

let has_tokens = assert false

let can_get_tokens = assert false

let add_tokens = assert false

let remove_tokens = assert false

let fire = assert false

let fire_all = assert false

let is_enabled = assert false

let places = assert false

let transitions = assert false

let arcs = assert false

let is_stuck = assert false
